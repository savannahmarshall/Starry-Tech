<h1>Your Star-filled Dashboard</h1>

<button id="new-post-btn" onclick="location.href='/posts/new'">Create a New Post</button>

{{#if posts.length}}
  <section class="posts-section">
    <h2>Your Posts</h2>
    {{#each posts}}
      <div class="post">
        <h3><a href="/posts/{{this.id}}">{{this.title}}</a></h3>
        <p>{{this.content}}</p>
        <small>Created on {{this.createdAt}}</small>
        <button data-id="{{this.id}}" class="edit-btn" onclick="location.href='/posts/{{this.id}}/edit'">Edit</button>
        <button data-id="{{this.id}}" class="delete-btn" onclick="location.href='/posts/{{this.id}}/delete'">Delete</button>
      </div>
    {{/each}}
  </section>
{{else}}
  <p>You haven't posted any blog entries yet.</p>
{{/if}}
